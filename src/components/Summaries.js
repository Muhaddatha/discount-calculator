import React from "react";

const Summaries = () => {
    return(
        <div id="summary-container">
            <ul className="summary-list"></ul>
        </div>
    );
}

export default Summaries;